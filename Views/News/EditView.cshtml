@{
    Layout.Title = "编辑新闻";
}
@model HYT.NewsDemo.ViewModels.NewsEditViewModel
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>EditView</title>
    <script src="http://libs.baidu.com/jquery/2.0.0/jquery.min.js"></script>
</head>
<body>
    @using (Html.BeginForm("edit", "news", FormMethod.Post))
    {
        <div>
            <input id="newsId" type="hidden" name="id" value="@Model.News.id" />
            新闻标题 <input id="title" type="text" name="title" value="@Model.News.newsTitle" /><br />
            新闻内容<input id="content" type="text" name="content" value="@Model.News.newsContent" /><br />
            @Html.AntiForgeryToken()
            <input id="btnSave" type="submit" value="保存" />
        </div>
    }
    @*<div>
            @using (Html.BeginForm("edit", "news", FormMethod.Post))
            {
                <div>
                    @Display(Model)
                    <input id="btnSave" type="submit" value="保存" />
                </div>
            }
        </div>*@
    <script>
                                            //$("#btnSave").on("click", function () {
                                            //    var data = { "title": $("#title").val(), "content": $("#content").val(), "Id": $("#newsId").val(), "__RequestVerificationToken": $("[name=__RequestVerificationToken]").val() };
                                            //    var headers = {};
                                            //    //headers["__RequestVerificationToken"] = $("[name=__RequestVerificationToken]").val();
                                            //    $.ajax({
                                            //        type: "POST",
                                            //        url: "/testdemo/news/edit",
                                            //        data: data,
                                            //        async: false,
                                            //        contentType: "application/x-www-form-urlencoded ",
                                            //        success: function (data) {
                                            //            alert("保存成功");
                                            //        },
                                            //        error: function () {
                                            //            alert("提交数据失败");
                                            //        }
                                            //    });
                                            //})
    </script>
</body>
</html>
